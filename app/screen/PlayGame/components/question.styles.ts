import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    justifyContent: 'center',
    alignItems: 'center',
    paddingHorizontal: 20,
  },
  contentQuestions: {
    padding: 10,
    backgroundColor: 'rgba(135, 139, 245, 0.5)',
    width: '100%',
    borderRadius: 10,
    justifyContent: 'center',
    alignItems: 'center',
  },
  textQuestions: {
    marginVertical: 20,
    color: 'white',
    fontSize: 25,
  },
  countDown: {
    marginVertical: 20,
    color: 'white',
    fontSize: 20,
  },
});

export default styles;
